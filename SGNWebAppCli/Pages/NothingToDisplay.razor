<div style="width:auto;height:auto;">
    @if (IsEnabled)
    {
        <p style="color:red; font-weight:bold">Brak rekordów spełniających zadane kryteria</p>
    }
    <nav class="nav nav-pills flex-column flex-sm-row">
        <a class="flex-sm-fill text-sm-center nav-link active" href="/reportstochoose" style="width:100px;height:40px">Wróć do poprzedniej storny</a>
    </nav>
</div>
<br />
@code {
    [Parameter] public bool IsEnabled { get; set; } = false;
}
